# %%
# import library
import numpy as np

import torch

from sklearn.metrics import accuracy_score, f1_score, confusion_matrix, plot_roc_curve, roc_curve, roc_auc_score

import matplotlib.pyplot as plt